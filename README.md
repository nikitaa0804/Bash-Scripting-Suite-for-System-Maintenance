# 🛠️ Bash Scripting Suite for System Maintenance

This project automates Linux system maintenance using Bash scripts.

## Features
- Automated backups (`backup.sh`)
- System updates and cleanup (`update_cleanup.sh`)
- Log monitoring for system errors (`log_monitor.sh`)
- Menu-based automation interface (`maintenance_menu.sh`)

## Usage
1. Download or clone this repository.
2. Give execution permission:
   ```bash
   chmod +x *.sh
